#include <stdio.h>
#include <math.h>

int main()
{
    int number =2;
    // int c= pow(number,2);
    // printf("square =%d",c);

    printf("square =%f",pow(number,2)); // %f bcoz it accept double data type  
                                        // pow func returns value in double data type 
}
